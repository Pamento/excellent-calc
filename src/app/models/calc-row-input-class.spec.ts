import { CalcRowInputClass } from './calc-row-input-class';

describe('CalcRowInputClass', () => {
  it('should create an instance', () => {
    expect(new CalcRowInputClass()).toBeTruthy();
  });
});
